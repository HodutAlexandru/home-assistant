spring:
  application:
    name: assistant

  ai:
    anthropic:
      api-key: ${ANTHROPIC-API-KEY}
      chat:
        options:
          model: ${CLAUDE-MODEL}

twilio:
  sid: ${TWILIO-SID}
  auth-token: ${TWILIO-TOKEN}

server:
  port: ${PORT:8080}
